### 说明文件

1.此目录存放管理员的skills内容,内容不跟随git仓库走

2.目录结构

```
skills_users/:admin_user_id/:skill_name/SKILL.md
skills_users/:admin_user_id/:skill_name/reference/xxx.md(非必须)
```

3.对eino框架不可显,仅用于SKILL管理,关联后复制到skills_robot/:robot_key目录
